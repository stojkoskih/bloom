# Bloom

Static React + Vite app configured for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Deploy to GitHub Pages

This repo is configured for `https://stojkoskih.github.io/bloom/`.

```bash
npm run deploy
```

After the first deploy, make sure the GitHub repository settings are using GitHub Pages from the `gh-pages` branch.
